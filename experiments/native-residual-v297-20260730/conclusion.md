# V297 Conclusion

Accepted. V297 adds a native phase-boundary cancellation summary for fully
joined workers that can terminate nondeterministically only after completing
an equal-opposite atomic update pair. One unified `deagle_exe` derives
admission, replaces only the proven identity transition words, runs the
ordinary backend, decides the property, and emits the correctness witness.
The byte-identical wrapper remains limited to routing and argument selection.

Exact725 reaches 700 official / 712 adjudicated correct, adding
`parallel-misc-3` and `parallel-misc-3-extended` with zero old-correct losses
and zero new wrong results. Prior90 remains 90/90. Against V296, aggregate CPU,
wall, and summed memory improve by 11.382%, 10.570%, and 2.003%. Both witnesses
pass WitnessLint's format gate, and the evaluated executable is reproduced
byte-for-byte by a second build.
